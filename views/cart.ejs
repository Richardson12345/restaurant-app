<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Menu</title>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }
        
        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }
        
        tr:nth-child(even) {
            background-color: #dddddd;
        }
        body {
              margin: 0;
              font-family: Arial, Helvetica, sans-serif;
            }
            table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
            }
        
            td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
            text-align: center
            }
            .topnav {
              overflow: hidden;
              background-color: #333;
            }
        
            .topnav a {
              float: left;
              color: #f2f2f2;
              text-align: center;
              padding: 14px 16px;
              text-decoration: none;
              font-size: 17px;
            }
        
            .topnav a:hover {
              background-color: #ddd;
              color: black;
            }
        
            .topnav a.active {
              background-color: #4CAF50;
              color: white;
            }
    </style>
</head>
<body>
    <div class="topnav">
        <a href="/user/menu">Menu</a>
        <a href="/user/cart">Cart</a>
        <a href="/user/edit">Edit User</a>
        <a href="/user/transaction">Transaction</a>
        <a href="/logout">Logout</a>
        </div>
        <div style="padding-left:16px">
    </div>
    <table>
          <tr>
            <th>No</th>
            <th>Nama Menu</th>
            <th>Harga</th>
            <th> </th>
          </tr>
          <%for(var i = 0; i < menuTrans.length; i++){%>
          <tr>
                <td><%=i+1%></td>
                <td><%=menuTrans[i].Menu.name%></td>
                <td><%=menuTrans[i].Menu.price%></td>
                <td><a href="/user/cart/delete/<%= menuTrans[i].id%>">Delete</a></td>
         </tr>
          <%}%>
    </table><br><br>
    <h4 style="color: red"><%=err%></h4>
    <br><br>

    <form action="/user/cart/voucher" method="POST">
        Total : <input type="text" readonly name="total" value="<%=total%>">
        <br><br>
        Voucher : <input type="text" readonly name="voucher" value="<%=voucher%>">
        <input type="submit" value="use"><br><br>
    </form>
    <form action="/user/cart/total" method="POST">
        Cash : <input type="number" name="cash"><br><br>
        Change : <%=change%>
        <br><br>
        <input type="submit" value="checkout">
    </form>
</body>
</html>